# ============================================================
# IAP – Intent-Aware Personalisation for Question Answering
# requirements.txt
# ============================================================

# Core deep-learning stack
torch>=2.2.0
transformers>=4.40.0
accelerate>=0.28.0
peft>=0.10.0                  # optional: for LoRA fine-tuning

# Fast inference (rollout sampling + evaluator)
vllm>=0.4.0

# Dataset loading
datasets>=2.19.0
huggingface-hub>=0.22.0

# Evaluation helpers
rouge-score>=0.1.2            # ROUGE-L contrastive penalty
json5>=0.9.14                 # robust JSON parsing in evaluator

# Tokenisation / text utilities
sentencepiece>=0.1.99
protobuf>=3.20.0

# Numeric / scientific
numpy>=1.26.0
scipy>=1.13.0

# Logging & config
tqdm>=4.66.0

# Flash Attention 2 (install separately – see README)
# flash-attn>=2.5.0           # pip install flash-attn --no-build-isolation
